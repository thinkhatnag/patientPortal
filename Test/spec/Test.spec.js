import EnterNewVitalsPage from "/Users/nagasubarayudu/Desktop/patientPortal/Test/screenObjectModel/enterNewVital.js";
import {
  swipe,
  verifyAndClick,
} from "/Users/nagasubarayudu/Desktop/patientPortal/helper/helper.js";
import RatingScalePage from "/Users/nagasubarayudu/Desktop/patientPortal/Test/screenObjectModel/ratingScale.js";
// describe('Testing the code', () => {
//   it('should run the test', async () => {
//     const totalOptions = 4; // Replace with actual count of radio buttons
//     const maxAttempts = 40; // Adjust as needed
//     let attempt = 0;

//     while (!(await RatingScalePage.addRatingScaleButton.isDisplayed()) || attempt < maxAttempts) {
//       attempt++;
//       const randomIndex = Math.floor(Math.random() * totalOptions); // 0 to 3
//       const option = await $(`android=new UiSelector().className("android.widget.RadioButton").instance(${randomIndex})`);
//       await option.click();

//       const text = await option.getText();
//       console.log(`Clicked on radio option: ${text}`);

//       if (await RatingScalePage.next.isDisplayed()) {
//         await verifyAndClick(RatingScalePage.next);
//       } else if (await RatingScalePage.submit.isDisplayed()) {
//         await verifyAndClick(RatingScalePage.submit);
//       } else {
//         console.log('No next or submit button found.');
//       }
//     }

//     if (await RatingScalePage.addRatingScaleButton.isDisplayed()) {
//       console.log('addRatingScaleButton is displayed.');
//       // Exit the loop if the button is found
//     } else {
//       console.log('Timeout: addRatingScaleButton not found after maximum attempts.');
//     }
//   });
// });
describe('Testing the code', () => {
  it('should run the test', async () => {


    
  });
});